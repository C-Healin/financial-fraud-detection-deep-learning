# Contributing

1. Create a virtual environment.
2. Install `requirements-dev.txt`.
3. Add tests for behaviour changes.
4. Run `ruff check src tests api scripts` and `pytest -q`.
5. Do not commit real customer or transaction data.
